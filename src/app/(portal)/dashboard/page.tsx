import SideNav from "@/src/components/ui/SideNav"

export default function Dashboard(){ 
    return ( 
        <main className="bg-surface-a0 min-h-screen flex">
            <SideNav />
        </main>
    )
}